/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ProductAttributePipePipe } from './product-attribute-pipe.pipe';

describe('Pipe: ProductAttributePipe', () => {
  it('create an instance', () => {
    let pipe = new ProductAttributePipePipe();
    expect(pipe).toBeTruthy();
  });
});
